sudo: require

services:
    - docker

branches:
  only:
    - master

before_install:
    - docker build -t tgvaughan/travis .

script:
    - docker run tgvaughan/travis /bin/bash -c 'cd /root; ant test'
